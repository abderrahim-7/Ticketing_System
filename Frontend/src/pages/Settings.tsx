import React from "react";
import GlobalLayout from "../layout/GlobalLayout";

const Settings = () => {
  return (
    <GlobalLayout>
      <div>Settings</div>
    </GlobalLayout>
  );
};

export default Settings;
